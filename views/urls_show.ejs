<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TinyApp</title>
  </head>
  
  <body>
    <header> <% include partials/_header %></header>
    <h1> Short URL: <%= shortURL %> </h1>
    <form method="POST" action="/urls/<%= shortURL %>">
      <input type="text" name="longURL" placeholder="<%= url["longURL"] %>" >
      <input type="submit" value="Update">
    </form>
  </body>
</html>