<!DOCTYPE html>
<html lang="en">

<head>
  <title>TinyApp</title>
</head>

  <body>
  <header>
    <% include partials/_header %>
  </header>
  <ul>
<% if (user && user.id) {
  for (let item in urls) { %>

    <li>
      <form method="POST" action="/urls/<%= item %>/delete">
        <a href="/u/<%= item %>"> <%= item %> </a>:
        <%= urls[item]["longURL"] %>
        <input type="submit" value="Delete">
        <button type="button" onclick="location.href='/urls/<%=item%>'" >Edit</button>
      </form>
    </li>

<%  }
}else { %>
  <h1>Please sign in or register to use the service....</h1>
<% } %>
  </ul>
  <a href="/urls/new">Create a New Short Link</a>
  </body>

</html>